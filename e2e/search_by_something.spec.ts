import { test, expect } from '@playwright/test';

test("Should search by something on search_bar", async ({ page }) => {
	// Wip: I need to write a simple test to check
	// if config file are consumed by app.
})
